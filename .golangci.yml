version: "2"

run:
  go: "1.26"
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - cyclop
  settings:
    cyclop:
      max-complexity: 18
  exclusions:
    rules:
      - linters:
          - errcheck
        path: _test\.go
