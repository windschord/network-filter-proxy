version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - cyclop
  settings:
    cyclop:
      max-complexity: 10

run:
  timeout: 5m
